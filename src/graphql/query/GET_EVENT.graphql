query GetEvent($getEventId: ID!) {
  getEvent(id: $getEventId) {
    _id
    name
    location
    date
    description
    banner
    tableLayout
    tickets {
      _id
      category
      price
      quantity
    }
  }
}
